[package]
name = "rlImGui"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
imgui = { path = "../imgui" }
raylib_wrapper = { path = "../raylib_wrapper" }

[build-dependencies]
cc = "1"
